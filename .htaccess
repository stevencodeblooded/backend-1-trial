# .htaccess file for semrush-backend directory
# This should be placed in the root of your semrush-backend folder

# Allow Authorization headers to pass through to PHP
RewriteEngine On

# Handle Authorization Header for Apache
RewriteCond %{HTTP:Authorization} .
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

# Alternative method - set environment variable
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1

# Enable CORS headers
Header always set Access-Control-Allow-Origin "*"
Header always set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
Header always set Access-Control-Max-Age "86400"

# Handle preflight OPTIONS requests
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^(.*)$ $1 [R=200,L]